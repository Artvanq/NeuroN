# Dependencies
node_modules/

# Environment & secrets (single root .env — see .env.example)
.env
.env.local
.env.*.local
.env.budget
.env.coolify
neuron_backend/neuron_backend/.env
neuron_frontend/.env.local

# Docker — local overrides / copies with real credentials (never commit)
docker-compose.override.yml
docker-compose.local.yml
docker-compose.*.local.yml
docker-compose.secrets.yml

# Build output
.next/
out/
dist/
build/

# Prisma
neuron_backend/neuron_backend/node_modules/.prisma/

# Logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Cursor assets (screenshots from chats)
assets/

# Test / coverage
coverage/
.nyc_output/

# Local media + DB backups (budget VPS)
neuron_backend/neuron_backend/data/
backups/
